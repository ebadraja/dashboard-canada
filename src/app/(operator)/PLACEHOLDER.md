# Operator View (Doc 3) — PLACEHOLDER

**Build order: 4th (last), after single-clinic is proven end-to-end.**

Empty on purpose. The highest-privilege control room + billing — the view that
turns this from a project into a resellable product.

Will contain (later, not now):

- **Add/configure a clinic by form, not code** (hours, slot template, plan,
  price, go live) — onboarding is data entry, never a new build.
- Manage/assign/disable VA and doctor users across clinics.
- **System health** across clinics; **cross-clinic analytics**.
- **Billing derived, not typed:** invoices generated from plan + usage;
  Draft → Sent → Paid/Overdue; recorded payments.
- The **two sides of money** meeting only here: revenue (doctor sees own) vs.
  cost (operator only) → margin (operator only), unreachable by any doctor/VA
  request by construction.
- Audit-log row on every operator action.

**Definition of done (Doc 3 §6):** create & fully configure a clinic by form and
go live; create/assign/disable users across clinics; invoices auto-generate from
plan + usage with tracked status/payments; only the operator sees cost & margin
per and across clinics; live system-health view; all operator actions audit-logged.
