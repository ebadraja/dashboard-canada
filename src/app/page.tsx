// Intentionally blank running page — this is the empty skeleton (Step 3).
// No features live here yet. Building order: Foundation (Doc 0), then the
// VA view (Doc 1), Doctor view (Doc 2), Operator view (Doc 3).
export default function Home() {
  return (
    <main>
      <h1>AI Receptionist</h1>
      <p>
        Empty skeleton is running. Nothing is built yet — the Foundation
        (Doc&nbsp;0) is the next slice. Health check:{" "}
        <a href="/api/health">/api/health</a>.
      </p>
    </main>
  );
}
